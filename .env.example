# Caminho base do app no deploy. Deixe vazio/relativo (padrão './') para raiz de
# domínio ou hospedagem estática comum. Para GitHub Pages em subdiretório, use:
#   VITE_BASE=/criativos-de-imagem/
VITE_BASE=./
